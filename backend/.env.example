# JWT Secret Key (generate a secure random string)
# In production, use: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=

# JWT Token Expiration (in minutes, default: 10080 = 7 days)
ACCESS_TOKEN_EXPIRE_MINUTES=10080

# CORS Allowed Origins (comma-separated list)
# For production, set to your frontend domain(s)
# Example: CORS_ORIGINS=https://example.com,https://www.example.com
# Leave empty for development (defaults to localhost ports)
CORS_ORIGINS=

# LLM API Configuration (optional, for LLM agents)
# OPENAI_API_KEY=your_openai_key
# ANTHROPIC_API_KEY=your_anthropic_key
# GEMINI_API_KEY=your_gemini_key
# LLM_API_KEY=your_llm_key
# LLM_MODEL=gpt-4
